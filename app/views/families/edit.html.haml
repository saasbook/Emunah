%h1 Edit Family

= form_tag family_path(@family), :method => :put do

  = label :family, :family_name, 'Family Name'
  = text_field :family, 'family_name'

  = submit_tag 'Update Family'

%table#families
  %thead
    %tr
      %th Person Name
      %th Person Information
  %tbody
    - @family.people.each do |person|
      %tr
        %td= person.full_name
        %td= link_to "See Person Info", show_person_path(person: person)

= button_to 'Add Person', new_person_path, :method => :get
